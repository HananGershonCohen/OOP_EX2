#include "Butten.h"

Butten::Butten(std::string str, sf::Vector2f position, sf::Color shapeColor, sf::Color textColor) : m_str(str) , m_position(position) , m_shapeColor (shapeColor) , m_textColor(textColor)
{
}
